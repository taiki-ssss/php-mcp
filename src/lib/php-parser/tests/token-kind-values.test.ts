import { describe, expect, test } from 'vitest';
import { TokenKind } from '../core/token';

describe('TokenKind Values', () => {
  test('check TokenKind enum values', () => {
    
    // Check if they are strings
  });
});